version: '3.9'
services:
  train:
    build:
      context: ../../..
      dockerfile: docker/segmentation/unet/Dockerfile.train

  test:
    build:
      context: ../../..
      dockerfile: docker/segmentation/unet/Dockerfile.test
    depends_on:
      train:
        condition: service_completed_successfully
